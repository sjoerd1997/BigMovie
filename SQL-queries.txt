A5:
SELECT TITLE, DURATION
FROM (list)
WHERE MAX(DURATION);

A9:
SELECT TITLE, LOCATION
FROM (list)
WHERE LOCATION LIKE 'New York';

A10:
SELECT TITLE, COUNT(COUNTRY)
FROM (list)
WHERE COUNTRY > 1;

A18:
SELECT TITLE, RATING, DURATION
FROM (list)
WHERE MIN(DURATION)
AND RATING >= 8.5;

A16:
SELECT TITLE, YEAR
FROM (list)
WHERE YEAR BETWEEN 1990 AND 2018;
...
